enum { zero (0), one(1), (255) } Binary;

       select (Binary) {
         case zero:
           uint8    zero_arm;
         case one:
           uint16   one_arm1;
           opaque   one_arm2[10];
       } named;
