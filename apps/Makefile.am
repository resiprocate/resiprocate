
SUBDIRS = clicktocall
SUBDIRS += sipdial
if BUILD_ICHAT_GW
SUBDIRS += ichat-gw
endif
if BUILD_TELEPATHY_CM
SUBDIRS += telepathy
endif
if BUILD_RECON
SUBDIRS += reConServer
endif
if BUILD_QPID_PROTON
if USE_NETSNMP
SUBDIRS += registration-agent
endif
endif

