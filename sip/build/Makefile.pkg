######################################################################

# include files from other directories.  right now, this is only the
# contrib directory, but there are a number that are there.  if we can
# move the rules out toward the edges, it will be easier for others to
# add elements.

######################################################################

ifneq ($(OSTYPE),FreeBSD)
PTHREAD_LIBNAME := pthread
else
PTHREAD_LDFLAGS := -pthread
PTHREAD_LIBNAME := c_r
endif

ifeq ($(OSTYPE),IRIX64)
PTHREAD_LDFLAGS := 
elif ($(OSCLASS),VXWORKS)
PTHREAD_LDFLAGS :=
PTHREAD_LIBNAME :=
elif ($(OSTYPE),Linux)
PTHREAD_LDFLAGS := #--thread
elif ($(OSTYPE),Solaris)
PTHREAD_LDFLAGS := --thread
elif ($(OSTYPE),QNX)
PTHREAD_DEFINES := USE_PTHREADS
PTHREAD_LDFLAGS := 
PTHREAD_LIBNAME :=
endif

OPENSSL_LIBNAME := ssl crypto
OPENSSL_LDFLAGS :=

ARES_INCLUDEDIRS := $(ROOT)/contrib/ares-1.1.1
ARES_LIBDIRS := $(ROOT)/contrib/ares-1.1.1
ARES_LIBNAME := ares

RESIPROCATE_INCLUDEDIRS := $(ROOT)/..

SIPSTACK2_INCLUDEDIRS := $(ROOT)/
SIPSTACK2_LIBDIRS := $(ROOT)/sipstack/obj.$(TARGET_NAME) 
SIPSTACK2_LIBNAME := sip2
SIPSTACK2_LDFLAGS :=
SIPSTACK2_DEPENDS := $(ROOT)/sipstack/obj.$(TARGET_NAME)/libsip2.so

$(SIPSTACK2_DEPENDS):
	cd $(ROOT)/sipstack;$(MAKE)


UTIL2_INCLUDEDIRS := $(ROOT)
UTIL2_LIBDIRS := $(ROOT)/util/obj.$(TARGET_NAME) 
UTIL2_LIBNAME := util2
UTIL2_LDFLAGS :=
UTIL2_DEPENDS := $(ROOT)/util/obj.$(TARGET_NAME)/libutil2.so

$(UTIL2_DEPENDS):
	cd $(ROOT)/util;$(MAKE)
