#!/bin/bash

set -e

./configure \
  --with-ssl \
  --with-mysql \
  --with-apps \
  --enable-dtls \
  --enable-ipv6 \
  --with-radius \
  --with-repro \
  --enable-repro-plugins \
  --with-rend \
  --with-c-ares

