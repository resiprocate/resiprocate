# Pin sipXtapi to a specific upstream commit for reproducible builds and CI
# caching.
#
# Both the Windows and Linux builds clone sipXtapi from GitHub at this commit
# via ExternalProject (see the top-level CMakeLists.txt), and the CI workflows
# key their sipXtapi caches on this file.  The Linux build ignores it when
# USE_SYSTEM_SIPXTAPI is on and links an installed libsipxtapi-dev instead.
#
# Replace the SHA below with the full 40-character sipXtapi commit SHA. Get
# the current upstream HEAD with:
#
#   git ls-remote https://github.com/sipXtapi/sipXtapi.git HEAD
#
# The build reads the first line that is nothing but hex digits and uses it as
# the ExternalProject GIT_TAG; comment lines (like these) are ignored. While no
# SHA is present, the build falls back to the default branch HEAD (unpinned).
#
# Commit Date: September 16, 2026
343c1256862a1f76e25322a736391e6953bb8c70
